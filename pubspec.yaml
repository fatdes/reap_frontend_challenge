name: reap_frontend_challenge
description: Reap frontend challenge

publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2

  openapi_generator_annotations: ^3.2.0
  insta_pic_api:
    path: ./generated_api/insta_pic

  equatable: ^2.0.3
  flutter_bloc: ^7.0.1

  image_picker: ^0.8.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  
  build_runner: any
  openapi_generator: ^3.2.0+2
  mockito: ^5.0.12
  mocktail: ^0.1.4
  bloc_test: ^8.0.2

flutter:
  uses-material-design: true
